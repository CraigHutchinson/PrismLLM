{
  "_prism_version": "1.0",
  "_note": "This file is managed by Prism (/prism hook on). Do not edit manually.",
  "_copilot_limitation": "userPromptSubmitted output is IGNORED by Copilot (observational only). Only preToolUse can block actions.",
  "hooks": [
    {
      "event": "userPromptSubmitted",
      "run": {
        "bash": "python PRISM_ROOT_PLACEHOLDER/hooks/prism_preparser.py --event userPromptSubmit --platform copilot",
        "powershell": "python PRISM_ROOT_PLACEHOLDER/hooks/prism_preparser.py --event userPromptSubmit --platform copilot"
      },
      "_purpose": "Logging and audit only — Copilot ignores output at prompt level"
    },
    {
      "event": "preToolUse",
      "run": {
        "bash": "python PRISM_ROOT_PLACEHOLDER/hooks/prism_preparser.py --event preToolUse --platform copilot --stdin",
        "powershell": "python PRISM_ROOT_PLACEHOLDER/hooks/prism_preparser.py --event preToolUse --platform copilot --stdin"
      },
      "_purpose": "Security gate: deny tool calls containing PII or injection patterns. Returns permissionDecision: deny."
    }
  ]
}
